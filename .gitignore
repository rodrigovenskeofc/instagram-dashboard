# Segredos e ambiente
.env
token.txt

# Python
__pycache__/
*.pyc

# Temporários
*.tmp
.DS_Store

# Banco SQLite do histórico — fica só no VPS (binário, não vai p/ repo público)
historico.db
historico.db-journal
historico.db-wal
historico.db-shm
_backup_drive_id.txt   # ID do arquivo de backup no Drive (config do VPS)

# Helpers locais (preview / seed) — só para uso local
_*.ps1
preview_local.html

# Projeto Flask antigo (substituído pela versão estática)
app.py
render.yaml
README_DESENVOLVEDOR.md
templates/

# segredos e dados da automacao da planilha
ig_state.json
gdrive_sa.json
.env
*.xlsx
capturar_log.txt
log_diario.txt
reels_coletados.json
